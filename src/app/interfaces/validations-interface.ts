export interface ValidationInterface {
    valor: string;
}

export interface ValidationIdInterface {
    id: string;
    valor: string;
}

export interface ValidationResponseInterface {
    present: boolean;
}
